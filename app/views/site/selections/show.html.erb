<p id="notice"><%= notice %></p>

<p>
  <strong>Post image:</strong>
  <%= @selection.post_image_id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @selection.name %>
</p>

<%= link_to 'Edit', edit_selection_path(@selection) %> |
<%= link_to 'Back', selections_path %>
