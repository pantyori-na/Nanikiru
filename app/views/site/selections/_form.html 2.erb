<%= form_with(model: selection, local: true) do |form| %>
  <% if selection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(selection.errors.count, "error") %> prohibited this selection from being saved:</h2>

      <ul>
      <% selection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_image_id %>
    <%= form.number_field :post_image_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
